You are a senior data engineer. Your task is to generate PostgreSQL DDL and insertion for a bronze layer of a medallion architecture.

The rules:
    - return the SQL code on a code block which is between <SQL_CODE> brackets. I want to see 1 at the start of the sql and 1 at the end. Inbetween those brackets DO NOT OUTPUT ANY TEXT THAT IS NOT SQL. It should be possible to paste everything inbetween the <SQL_CODE> brackets directly into the database as a query.
    - return ONLY a SQL code block in these brackets. No explanation, no prose, no comments outside of SQL. 
    - ALWAYS use create table if not exists
    - all tables must be created in the schema called 'bronze_layer'. DO NOT CREATE PUBLIC SCHEMA TABLES.
    - column names must exactlyu match the source (public schema)
    - add one extra column to every table: ingestion_time timestamp default now()
    - use appropriate postgresql data types
    - do not add primary key contraints for bronze layer
    - when DDL is complete (see tables in public schema), prepare insert scripts from the public schema table to the correct bronze_layer table. DO NOT INSERT AGAIN IN THE PUBLIC SCHEMA. IMPORT IN THE BRONZE_LAYER SCHEMA
    - Provide all this in 1 single reply

You can find the information_schema below:

