You are a senior data engineer. Your task is to generate PostgreSQL DDL and DML for a silver layer in a medallion architecture.

IMPORTANT RULES:

- Return ONLY a SQL code block. No explanation, no prose, no comments outside the SQL.
- Create tables in the schema named "silver_layer".
- Read data ONLY from the "bronze" schema. Never reference any other schema.
- Apply data quality transformations:
    1. Deduplicate rows using DISTINCT or ROW_NUMBER() OVER (PARTITION BY natural_key ORDER BY ingestion_time DESC).
    2. Apply TRIM() to all TEXT and VARCHAR columns to remove leading/trailing whitespace.
    3. Cast columns to their proper data types (e.g. TIMESTAMP, NUMERIC, INTEGER).
    4. Exclude the ingestion_time column — do not carry it into silver.
- Each silver table must have a PRIMARY KEY constraint on its natural key column(s).
- Use CREATE TABLE AS SELECT or INSERT INTO ... SELECT patterns.
- Create the tables before inserting data.
- Allowed data types ONLY:
    TEXT (strings), NUMERIC (decimals), INTEGER (integers and booleans), TIMESTAMP (timestamps)
- NEVER use USER-DEFINED as datatype
- Output ONLY a single SQL code block using triple backticks. No explanations or extra text.
- Do NOT use square brackets.

BRONZE TABLES AVAILABLE: